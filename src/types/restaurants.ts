export interface IRestaurant {
    id: string
    owner_user_id: number
    name: string
    description: string
    address: string
    is_open: boolean
    created_at: string
    restaurant_img: string
}